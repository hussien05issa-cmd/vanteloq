import { and, eq } from "drizzle-orm";
import { businessDateForTimestamp } from "../../../../../../domain/intraday-sales";
import { getD1, getDb } from "../../../../../../db";
import { integrationConnections, integrationLocationMappings, integrationSyncRuns } from "../../../../../../db/schema";
import { scopeExternalRef, unscopedExternalRef } from "../../../../../../domain/integration-source";
import { recordAudit } from "../../../../../../server/audit";
import { requireAccess } from "../../../../../../server/authorization";
import { ApiError, enforceRateLimit, handleApi, jsonResponse, requireSameOrigin } from "../../../../../../server/api";
import { acquireIntegrationSyncLease, releaseIntegrationSyncLease, renewIntegrationSyncLease, requireOwnedIntegrationConnection, sqliteTimestampSeconds } from "../../../../../../server/integrations/connection";
import { SHOPIFY_ONLINE_LOCATION_REF, SHOPIFY_POS_PROVIDER, shopifyGraphql, shopifyProviderFromRequest, shopifySha256 } from "../../../../../../server/integrations/shopify-pos";
import { applyOwnerInventoryCosts } from "../../../../../../server/inventory-costs";
import { requirePermission } from "../../../../../../server/permissions";

const QUERY = `query VanteloqShopifySync($orderQuery: String!, $ordersAfter: String, $productsAfter: String, $customersAfter: String, $ordersEnabled: Boolean!, $productsEnabled: Boolean!, $customersEnabled: Boolean!) {
  orders(first: 100, after: $ordersAfter, sortKey: CREATED_AT, reverse: true, query: $orderQuery) @include(if: $ordersEnabled) {
    nodes { id name sourceName createdAt updatedAt displayFinancialStatus cancelledAt retailLocation { id } customer { id displayName firstName lastName email phone }
      currentTotalPriceSet { shopMoney { amount } } currentSubtotalPriceSet { shopMoney { amount } } currentTotalTaxSet { shopMoney { amount } } currentTotalDiscountsSet { shopMoney { amount } } currentTotalRefundedSet { shopMoney { amount } }
      lineItems(first: 100) { nodes { id name quantity discountedTotalSet { shopMoney { amount } } totalDiscountSet { shopMoney { amount } } variant { id sku product { title } } } }
      transactions { id kind status gateway processedAt amountSet { shopMoney { amount } } }
    } pageInfo { hasNextPage endCursor }
  }
  products(first: 100, after: $productsAfter, sortKey: UPDATED_AT) @include(if: $productsEnabled) {
    nodes { id title vendor productType status updatedAt variants(first: 100) { nodes { id sku title price inventoryQuantity inventoryItem { unitCost { amount } inventoryLevels(first: 50) { nodes { location { id name } quantities(names: ["available"]) { name quantity } } } } } } }
    pageInfo { hasNextPage endCursor }
  }
  customers(first: 100, after: $customersAfter, sortKey: UPDATED_AT) @include(if: $customersEnabled) { nodes { id displayName firstName lastName email phone updatedAt } pageInfo { hasNextPage endCursor } }
}`;

type Money = { amount?: string | null };
type Page<T> = { nodes: T[]; pageInfo: { hasNextPage: boolean; endCursor: string | null } };
type Customer = { id: string; displayName?: string | null; firstName?: string | null; lastName?: string | null; email?: string | null; phone?: string | null; updatedAt?: string | null };
type Order = { id: string; name?: string; sourceName?: string | null; createdAt: string; updatedAt: string; displayFinancialStatus?: string | null; cancelledAt?: string | null; retailLocation?: { id: string } | null; customer?: Customer | null; currentTotalPriceSet?: { shopMoney?: Money }; currentSubtotalPriceSet?: { shopMoney?: Money }; currentTotalTaxSet?: { shopMoney?: Money }; currentTotalDiscountsSet?: { shopMoney?: Money }; currentTotalRefundedSet?: { shopMoney?: Money }; lineItems: { nodes: Array<{ id: string; name: string; quantity: number; discountedTotalSet?: { shopMoney?: Money }; totalDiscountSet?: { shopMoney?: Money }; variant?: { id: string; sku?: string | null; product?: { title?: string | null } | null } | null }> }; transactions: Array<{ id: string; kind?: string; status?: string; gateway?: string | null; processedAt?: string | null; amountSet?: { shopMoney?: Money } }> };
type Product = { id: string; title: string; vendor?: string | null; productType?: string | null; status?: string; updatedAt?: string; variants: { nodes: Array<{ id: string; sku?: string | null; title?: string | null; price?: string | null; inventoryQuantity?: number | null; inventoryItem?: { unitCost?: Money | null; inventoryLevels?: { nodes: Array<{ location: { id: string; name: string }; quantities: Array<{ name: string; quantity: number }> }> } | null } | null }> } };
type SyncData = { orders?: Page<Order>; products?: Page<Product>; customers?: Page<Customer> };
type Checkpoint = { version: 1; orders: string | null; products: string | null; customers: string | null; complete: { orders: boolean; products: boolean; customers: boolean } };
const emptyCheckpoint = (): Checkpoint => ({ version: 1, orders: null, products: null, customers: null, complete: { orders: false, products: false, customers: false } });
function parseCheckpoint(value: string | null): Checkpoint { if (!value) return emptyCheckpoint(); try { const parsed = JSON.parse(value) as Checkpoint; return parsed.version === 1 && parsed.complete ? parsed : emptyCheckpoint(); } catch { return emptyCheckpoint(); } }
function cents(value: unknown) { const amount = typeof value === "string" ? Number(value) : Number(value ?? 0); return Number.isFinite(amount) ? Math.max(0, Math.round(amount * 100)) : 0; }
function tender(gateway?: string | null): "cash" | "card" | "gift_card" | "store_credit" | "other" { const name = (gateway ?? "").toLowerCase(); if (name.includes("cash")) return "cash"; if (name.includes("gift")) return "gift_card"; if (name.includes("credit") && name.includes("store")) return "store_credit"; if (name && !name.includes("manual")) return "card"; return "other"; }
async function batches(statements: D1PreparedStatement[]) { let changed = 0; for (let index = 0; index < statements.length; index += 75) { const result = await getD1().batch(statements.slice(index, index + 75)); changed += result.reduce((sum, row) => sum + Number(row.meta.changes ?? 0), 0); } return changed; }

export async function POST(request: Request) {
  return handleApi(request, async ({ requestId }) => {
    const provider = shopifyProviderFromRequest(request); const isCommerce = provider !== SHOPIFY_POS_PROVIDER; const providerLabel = isCommerce ? "Shopify e-commerce" : "Shopify POS";
    requireSameOrigin(request); const context = await requireAccess(request, ["owner", "admin"], "pos.reporting.core"); await requirePermission(context, "integrations.manage"); const input = await request.json().catch(() => ({})) as { connectionId?: unknown; reason?: unknown }; const reason = input.reason === "manual" ? "manual" : "auto";
    const connection = await requireOwnedIntegrationConnection(context.organizationId, provider, typeof input.connectionId === "string" ? input.connectionId : null, { connected: true });
    if (!connection.domainPrefix) throw new ApiError(409, "SHOPIFY_NOT_CONNECTED", "Reconnect the Shopify store before synchronizing it.");
    if (reason === "auto") return jsonResponse({ provider, connectionId: connection.id, skipped: true, nextStep: `${providerLabel} refreshes remain owner-initiated until the durable worker schedule is enabled.` });
    await enforceRateLimit(`${provider}:manual-sync`, context.organizationId, 30, 3600);
    const lease = await acquireIntegrationSyncLease(context.organizationId, provider, connection.id);
    if (!lease) return jsonResponse({ provider: provider, connectionId: connection.id, coalesced: true, nextStep: "This Shopify store is already synchronizing." });
    const startedAt = new Date(); const runId = `${provider}-${crypto.randomUUID()}`; const importId = `provider-${provider}-${runId}`; const previous = parseCheckpoint(connection.lastSyncCursor); const database = getD1();
    try {
      await database.prepare(`INSERT INTO integration_sync_runs (id, organization_id, provider, connection_id, mode, status, cursor_before, records_read, records_staged, duplicates_skipped, warning_count, started_at, created_by_user_id) VALUES (?, ?, ?, ?, 'incremental', 'running', ?, 0, 0, 0, 0, ?, ?)`)
        .bind(runId, context.organizationId, provider, connection.id, connection.lastSyncCursor, sqliteTimestampSeconds(startedAt.getTime()), context.userId).run();
      const responseData = await shopifyGraphql<SyncData>(context.organizationId, connection.id, connection.domainPrefix, QUERY, {
        orderQuery: isCommerce ? "source_name:web" : "source_name:pos",
        ordersAfter: previous.orders,
        productsAfter: previous.products,
        customersAfter: previous.customers,
        ordersEnabled: !previous.complete.orders,
        productsEnabled: !previous.complete.products,
        customersEnabled: !previous.complete.customers,
      }, fetch, provider);
      const emptyPage = <T,>(): Page<T> => ({ nodes: [], pageInfo: { hasNextPage: false, endCursor: null } });
      const data = {
        orders: responseData.orders ?? emptyPage<Order>(),
        products: responseData.products ?? emptyPage<Product>(),
        customers: responseData.customers ?? emptyPage<Customer>(),
      };
      await renewIntegrationSyncLease(lease);
      const mappings = await getDb().select().from(integrationLocationMappings).where(and(eq(integrationLocationMappings.organizationId, context.organizationId), eq(integrationLocationMappings.provider, provider), eq(integrationLocationMappings.connectionId, connection.id)));
      const mapped = new Set(mappings.filter((row) => row.status === "mapped").map((row) => row.externalLocationRef)); const unmapped = mappings.filter((row) => row.status === "unmapped").length; const scoped = (value: string | null) => scopeExternalRef(connection.sourceNamespace, value); const now = Date.now();
      const suppliers = [...new Set(data.products.nodes.map((product) => product.vendor?.trim()).filter((value): value is string => Boolean(value)))];
      const supplierStatements = await Promise.all(suppliers.map(async (name) => database.prepare(`INSERT INTO commerce_suppliers (id, organization_id, provider, connection_id, external_supplier_id, name, archived, source_updated_at, source_payload_hash, sync_run_id, updated_at) VALUES (?, ?, ?, ?, ?, ?, 0, NULL, ?, ?, ?) ON CONFLICT(organization_id, provider, connection_id, external_supplier_id) DO UPDATE SET name=excluded.name, source_payload_hash=excluded.source_payload_hash, sync_run_id=excluded.sync_run_id, updated_at=excluded.updated_at`).bind(crypto.randomUUID(), context.organizationId, provider, connection.id, scoped(name.toLowerCase()), name.slice(0, 160), await shopifySha256(name), runId, now)));
      const importedSuppliers = await batches(supplierStatements);
      const variants = data.products.nodes.flatMap((product) => product.variants.nodes.map((variant) => ({ product, variant })));
      const productStatements = await Promise.all(variants.map(async ({ product, variant }) => { const external = scoped(variant.id); const sku = variant.sku?.trim() || variant.id.split("/").at(-1) || variant.id; const name = product.variants.nodes.length > 1 ? `${product.title} · ${variant.title || sku}` : product.title; const hash = await shopifySha256(JSON.stringify([product.updatedAt, variant.id, variant.price, variant.inventoryItem?.unitCost?.amount])); return database.prepare(`INSERT INTO commerce_products (id, organization_id, provider, connection_id, external_product_id, sku, name, category_ref, supplier_ref, default_cost_cents, default_price_cents, archived, source_updated_at, source_payload_hash, sync_run_id, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?) ON CONFLICT(organization_id, provider, connection_id, external_product_id) DO UPDATE SET sku=excluded.sku, name=excluded.name, category_ref=excluded.category_ref, supplier_ref=excluded.supplier_ref, default_cost_cents=excluded.default_cost_cents, default_price_cents=excluded.default_price_cents, archived=excluded.archived, source_updated_at=excluded.source_updated_at, source_payload_hash=excluded.source_payload_hash, sync_run_id=excluded.sync_run_id, updated_at=excluded.updated_at`).bind(crypto.randomUUID(), context.organizationId, provider, connection.id, external, sku.slice(0, 160), name.slice(0, 240), scoped(product.productType || null), scoped(product.vendor?.toLowerCase() || null), variant.inventoryItem?.unitCost?.amount ? cents(variant.inventoryItem.unitCost.amount) : null, cents(variant.price), product.status === "ARCHIVED" ? 1 : 0, product.updatedAt || null, hash, runId, now); }));
      const importedProducts = await batches(productStatements);
      const customersById = new Map<string, Customer>(); for (const customer of data.customers.nodes) customersById.set(customer.id, customer); for (const order of data.orders.nodes) if (order.customer?.id) customersById.set(order.customer.id, order.customer);
      const customerStatements = await Promise.all([...customersById.values()].map(async (customer) => { const display = customer.displayName?.trim() || [customer.firstName, customer.lastName].filter(Boolean).join(" ") || "Shopify customer"; return database.prepare(`INSERT INTO commerce_customers (id, organization_id, provider, connection_id, external_customer_id, display_name, first_name, last_name, email, phone, archived, source_updated_at, source_payload_hash, sync_run_id, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 0, ?, ?, ?, ?) ON CONFLICT(organization_id, provider, connection_id, external_customer_id) DO UPDATE SET display_name=excluded.display_name, first_name=excluded.first_name, last_name=excluded.last_name, email=excluded.email, phone=excluded.phone, source_updated_at=excluded.source_updated_at, source_payload_hash=excluded.source_payload_hash, sync_run_id=excluded.sync_run_id, updated_at=excluded.updated_at`).bind(crypto.randomUUID(), context.organizationId, provider, connection.id, scoped(customer.id), display.slice(0, 200), customer.firstName?.slice(0, 120) || null, customer.lastName?.slice(0, 120) || null, customer.email?.slice(0, 320) || null, customer.phone?.slice(0, 64) || null, customer.updatedAt || null, await shopifySha256(JSON.stringify([customer.id, customer.updatedAt, display])), runId, now); }));
      const importedCustomers = await batches(customerStatements);
      const completedOrders = data.orders.nodes.filter((order) => !order.cancelledAt && (isCommerce || Boolean(order.retailLocation?.id)) && ["PAID", "PARTIALLY_REFUNDED", "REFUNDED", "PARTIALLY_PAID"].includes(order.displayFinancialStatus ?? ""));
      const lineStatements: D1PreparedStatement[] = []; const paymentStatements: D1PreparedStatement[] = []; const stageStatements: D1PreparedStatement[] = [];
      for (const order of completedOrders) { const location = isCommerce ? SHOPIFY_ONLINE_LOCATION_REF : order.retailLocation!.id; const orderHash = await shopifySha256(JSON.stringify([order.id, order.updatedAt, order.currentTotalPriceSet, order.currentTotalRefundedSet])); const total = cents(order.currentTotalPriceSet?.shopMoney?.amount); const tax = cents(order.currentTotalTaxSet?.shopMoney?.amount); const discount = cents(order.currentTotalDiscountsSet?.shopMoney?.amount); stageStatements.push(database.prepare(`INSERT INTO integration_staged_sales (id, organization_id, provider, connection_id, external_sale_id, external_version, outlet_ref, sold_at, state, total_cents, tax_cents, cost_cents, discount_cents, line_count, source_payload_hash, sync_run_id, staged_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 0, ?, ?, ?, ?, ?) ON CONFLICT(organization_id, provider, connection_id, external_sale_id, external_version) DO NOTHING`).bind(crypto.randomUUID(), context.organizationId, provider, connection.id, scoped(order.id), order.updatedAt, scoped(location), order.createdAt, order.displayFinancialStatus || "PAID", total, tax, discount, order.lineItems.nodes.length, orderHash, runId, now));
        if (!mapped.has(location)) continue;
        for (const line of order.lineItems.nodes) lineStatements.push(database.prepare(`INSERT INTO commerce_sale_lines (id, organization_id, provider, connection_id, external_sale_id, external_line_id, product_ref, customer_ref, outlet_ref, sold_at, sku, product_name, quantity_milli, net_sales_cents, cost_cents, discount_cents, source_payload_hash, sync_run_id, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 0, ?, ?, ?, ?) ON CONFLICT(organization_id, provider, connection_id, external_sale_id, external_line_id) DO UPDATE SET product_ref=excluded.product_ref, customer_ref=excluded.customer_ref, outlet_ref=excluded.outlet_ref, sold_at=excluded.sold_at, sku=excluded.sku, product_name=excluded.product_name, quantity_milli=excluded.quantity_milli, net_sales_cents=excluded.net_sales_cents, discount_cents=excluded.discount_cents, source_payload_hash=excluded.source_payload_hash, sync_run_id=excluded.sync_run_id, updated_at=excluded.updated_at`).bind(crypto.randomUUID(), context.organizationId, provider, connection.id, scoped(order.id), scoped(line.id), scoped(line.variant?.id || null), scoped(order.customer?.id || null), scoped(location), order.createdAt, line.variant?.sku || null, (line.variant?.product?.title || line.name).slice(0, 240), Math.max(0, Math.round(line.quantity * 1000)), cents(line.discountedTotalSet?.shopMoney?.amount), cents(line.totalDiscountSet?.shopMoney?.amount), await shopifySha256(JSON.stringify([order.updatedAt, line])), runId, now));
        for (const transaction of order.transactions.filter((item) => item.status === "SUCCESS")) { const isRefund = transaction.kind === "REFUND"; const gateway = (transaction.gateway || "Other").slice(0, 100); paymentStatements.push(database.prepare(`INSERT INTO commerce_payments (id, organization_id, provider, connection_id, external_payment_id, external_sale_id, payment_type_ref, payment_type_name, category, amount_cents, paid_at, outlet_ref, source_payload_hash, sync_run_id, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?) ON CONFLICT(organization_id, provider, connection_id, external_payment_id) DO UPDATE SET amount_cents=excluded.amount_cents, paid_at=excluded.paid_at, payment_type_name=excluded.payment_type_name, source_payload_hash=excluded.source_payload_hash, sync_run_id=excluded.sync_run_id, updated_at=excluded.updated_at`).bind(crypto.randomUUID(), context.organizationId, provider, connection.id, scoped(transaction.id), scoped(order.id), scoped(`${isRefund ? "refund:" : ""}${transaction.gateway || "other"}`), `${isRefund ? "Refund · " : ""}${gateway}`.slice(0, 120), tender(transaction.gateway), cents(transaction.amountSet?.shopMoney?.amount), transaction.processedAt || order.createdAt, scoped(location), await shopifySha256(JSON.stringify(transaction)), runId, now)); }
      }
      const importedLines = await batches(lineStatements); const importedPayments = await batches(paymentStatements); const stagedSales = await batches(stageStatements);
      const inventoryStatements: D1PreparedStatement[] = []; for (const { product, variant } of variants) { const sku = variant.sku?.trim() || variant.id.split("/").at(-1) || variant.id; for (const level of variant.inventoryItem?.inventoryLevels?.nodes ?? []) { if (!mapped.has(level.location.id)) continue; const available = level.quantities.find((quantity) => quantity.name === "available")?.quantity ?? variant.inventoryQuantity ?? 0; inventoryStatements.push(database.prepare(`INSERT INTO inventory_balances (id, organization_id, location_ref, sku, name, on_hand_quantity, reorder_point, version, source_provider, source_connection_id, updated_at) VALUES (?, ?, ?, ?, ?, ?, 0, 1, ?, ?, ?) ON CONFLICT(organization_id, location_ref, sku) DO UPDATE SET name=excluded.name, on_hand_quantity=excluded.on_hand_quantity, version=inventory_balances.version+1, source_provider=excluded.source_provider, source_connection_id=excluded.source_connection_id, updated_at=excluded.updated_at`).bind(crypto.randomUUID(), context.organizationId, `${provider}:${scoped(level.location.id)}`, sku.slice(0, 160), product.title.slice(0, 240), Math.max(0, available), provider, connection.id, now)); } }
      const importedInventory = await batches(inventoryStatements);
      await database.prepare(`UPDATE commerce_sale_lines AS line SET cost_cents=COALESCE((SELECT ROUND(COALESCE(product.owner_cost_cents, product.default_cost_cents) * line.quantity_milli / 1000.0) FROM commerce_products product WHERE product.organization_id=line.organization_id AND product.provider=line.provider AND product.connection_id=line.connection_id AND product.external_product_id=line.product_ref), 0) WHERE line.organization_id=? AND line.provider=? AND line.connection_id=?`).bind(context.organizationId, provider, connection.id).run();
      await database.prepare(`UPDATE integration_staged_sales AS sale SET cost_cents=COALESCE((SELECT SUM(line.cost_cents) FROM commerce_sale_lines line WHERE line.organization_id=sale.organization_id AND line.provider=sale.provider AND line.connection_id=sale.connection_id AND line.external_sale_id=sale.external_sale_id), 0) WHERE sale.organization_id=? AND sale.provider=? AND sale.connection_id=?`).bind(context.organizationId, provider, connection.id).run();
      await applyOwnerInventoryCosts(context.organizationId, connection.id, now);
      const missing = await database.prepare(`SELECT COUNT(*) count FROM commerce_sale_lines line LEFT JOIN commerce_products product ON product.organization_id=line.organization_id AND product.provider=line.provider AND product.connection_id=line.connection_id AND product.external_product_id=line.product_ref WHERE line.organization_id=? AND line.provider=? AND line.connection_id=? AND line.net_sales_cents>0 AND (line.product_ref IS NULL OR (product.owner_cost_cents IS NULL AND product.default_cost_cents IS NULL))`).bind(context.organizationId, provider, connection.id).first<{ count: number }>(); const missingCostCount = Number(missing?.count ?? 0);
      const completedAt = new Date(); const pageComplete = { orders: !data.orders.pageInfo.hasNextPage, products: !data.products.pageInfo.hasNextPage, customers: !data.customers.pageInfo.hasNextPage }; const allComplete = pageComplete.orders && pageComplete.products && pageComplete.customers; const cursor = JSON.stringify(allComplete ? emptyCheckpoint() : { version: 1, orders: data.orders.pageInfo.endCursor, products: data.products.pageInfo.endCursor, customers: data.customers.pageInfo.endCursor, complete: pageComplete } satisfies Checkpoint); const warningCount = unmapped + missingCostCount; const recordsRead = data.orders.nodes.length + data.products.nodes.length + data.customers.nodes.length; const recordsStaged = stagedSales + importedProducts + importedCustomers + importedSuppliers + importedLines + importedPayments + importedInventory;
      const publishRequested = Boolean(connection.promotionAuthorizedAt || connection.dataPromotionStatus === "approved"); const publishCanonical = publishRequested && unmapped === 0 && missingCostCount === 0;
      let publishedMetrics = 0; await database.prepare(`INSERT INTO data_imports (id, organization_id, import_type, status, file_name, row_count, idempotency_key, imported_by_user_id, created_at) VALUES (?, ?, 'manual_entry', 'processing', ?, 0, ?, ?, ?)`).bind(importId, context.organizationId, `${providerLabel} read-only sync`, runId, context.userId, now).run();
      if (publishCanonical) { const latest = await database.prepare(`SELECT outlet_ref outletRef, sold_at soldAt, total_cents totalCents, cost_cents costCents, discount_cents discountCents, line_count lineCount FROM (SELECT *, row_number() OVER (PARTITION BY external_sale_id ORDER BY staged_at DESC, id DESC) version_rank FROM integration_staged_sales WHERE organization_id=? AND provider=? AND connection_id=?) WHERE version_rank=1`).bind(context.organizationId, provider, connection.id).all<{ outletRef: string; soldAt: string; totalCents: number; costCents: number; discountCents: number; lineCount: number }>(); const groups = new Map<string, { date: string; location: string; total: number; cost: number; refunds: number; discounts: number; transactions: number; units: number }>(); for (const sale of latest.results ?? []) { const date = businessDateForTimestamp(sale.soldAt, context.organization.timezone); if (!date) continue; const raw = unscopedExternalRef(connection.sourceNamespace, sale.outletRef) ?? sale.outletRef; const key = `${date}:${raw}`; const row = groups.get(key) ?? { date, location: `${provider}:${scoped(raw)}`, total: 0, cost: 0, refunds: 0, discounts: 0, transactions: 0, units: 0 }; row.total += sale.totalCents; row.cost += sale.costCents; row.discounts += sale.discountCents; row.transactions += 1; row.units += sale.lineCount; groups.set(key, row); } const refunds = await database.prepare(`SELECT paid_at paidAt, outlet_ref outletRef, amount_cents amountCents FROM commerce_payments WHERE organization_id=? AND provider=? AND connection_id=? AND payment_type_name LIKE 'Refund · %'`).bind(context.organizationId, provider, connection.id).all<{ paidAt: string; outletRef: string; amountCents: number }>(); for (const refund of refunds.results ?? []) { const date = businessDateForTimestamp(refund.paidAt, context.organization.timezone); if (!date) continue; const raw = unscopedExternalRef(connection.sourceNamespace, refund.outletRef) ?? refund.outletRef; const key = `${date}:${raw}`; const row = groups.get(key) ?? { date, location: `${provider}:${scoped(raw)}`, total: 0, cost: 0, refunds: 0, discounts: 0, transactions: 0, units: 0 }; row.refunds += refund.amountCents; groups.set(key, row); } await database.prepare(`DELETE FROM daily_business_metrics WHERE organization_id=? AND source_connection_id=?`).bind(context.organizationId, connection.id).run(); for (const row of groups.values()) { await database.prepare(`INSERT INTO daily_business_metrics (organization_id, business_date, location_ref, gross_sales_cents, net_sales_cents, cost_of_goods_cents, transaction_count, units_sold, refunds_cents, discounts_cents, labour_cost_cents, source_provider, source_connection_id, source_import_id, created_by_user_id, created_at, updated_at) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 0, ?, ?, ?, ?, ?, ?) ON CONFLICT(organization_id, business_date, location_ref) DO UPDATE SET gross_sales_cents=excluded.gross_sales_cents, net_sales_cents=excluded.net_sales_cents, cost_of_goods_cents=excluded.cost_of_goods_cents, transaction_count=excluded.transaction_count, units_sold=excluded.units_sold, refunds_cents=excluded.refunds_cents, discounts_cents=excluded.discounts_cents, source_provider=excluded.source_provider, source_connection_id=excluded.source_connection_id, source_import_id=excluded.source_import_id, updated_at=excluded.updated_at`).bind(context.organizationId, row.date, row.location, row.total + row.discounts + row.refunds, row.total, row.cost, row.transactions, row.units, row.refunds, row.discounts, provider, connection.id, importId, context.userId, now, now).run(); } publishedMetrics = groups.size; }
      await database.prepare(`UPDATE data_imports SET status='completed', row_count=? WHERE id=? AND organization_id=?`).bind(recordsStaged + publishedMetrics, importId, context.organizationId).run(); await getDb().update(integrationSyncRuns).set({ status: "completed", cursorAfter: cursor, recordsRead, recordsStaged, warningCount, completedAt }).where(eq(integrationSyncRuns.id, runId)); await renewIntegrationSyncLease(lease);
      const [updated] = await getDb().update(integrationConnections).set({ lastSuccessfulSyncAt: completedAt, lastSyncCursor: cursor, dataPromotionStatus: publishCanonical ? "approved" : "staging", promotionAuthorizedAt: publishCanonical ? null : connection.promotionAuthorizedAt, lastErrorCode: unmapped ? "SHOPIFY_LOCATION_UNMAPPED" : missingCostCount ? "SHOPIFY_PRODUCT_COST_REQUIRED" : null, syncLeaseOwner: null, syncLeaseExpiresAt: null, updatedAt: completedAt }).where(and(eq(integrationConnections.id, connection.id), eq(integrationConnections.syncLeaseOwner, lease.owner), eq(integrationConnections.syncVersion, lease.version))).returning({ id: integrationConnections.id }); if (!updated) throw new ApiError(409, "INTEGRATION_SYNC_LEASE_LOST", `The ${providerLabel} sync was superseded before it could publish.`);
      await recordAudit({ request, requestId, organizationId: context.organizationId, actorUserId: context.userId, action: "integration.data_imported", resourceType: "integration_sync_run", resourceId: runId, details: { provider: provider, connectionId: connection.id, recordsRead, recordsStaged, products: importedProducts, customers: importedCustomers, suppliers: importedSuppliers, inventory: importedInventory, saleLines: importedLines, payments: importedPayments, missingCostCount, unmappedLocations: unmapped, dataPromotionEnabled: publishCanonical } });
      return jsonResponse({ provider, connectionId: connection.id, run: { id: runId, status: "completed", recordsRead, recordsStaged, duplicatesSkipped: 0, warningCount }, reconciliation: { orders: completedOrders.length, saleLines: lineStatements.length, payments: paymentStatements.length, refunds: paymentStatements.length ? completedOrders.reduce((sum, order) => sum + order.transactions.filter((item) => item.status === "SUCCESS" && item.kind === "REFUND").length, 0) : 0, products: variants.length, customers: customersById.size, suppliers: suppliers.length, inventoryBalances: inventoryStatements.length, locations: mappings.length, unmappedLocations: unmapped, missingItemCosts: missingCostCount, dailyMetrics: publishedMetrics }, readyForReview: completedOrders.length > 0 && warningCount === 0 && allComplete, stagingOnly: !publishCanonical, dataPromotionEnabled: publishCanonical, nextStep: !allComplete ? `Continue the resumable ${providerLabel} history import.` : unmapped ? `Map every ${providerLabel} location, then review the import.` : missingCostCount ? "Add verified item costs in Shopify or Vanteloq before profit is published." : publishCanonical ? `${providerLabel} data is synchronized and available across Vanteloq.` : `Review and approve this ${providerLabel} import, then run one final sync to publish it.` });
    } catch (error) { const code = error instanceof ApiError ? error.code : "SHOPIFY_SYNC_FAILED"; await getDb().update(integrationSyncRuns).set({ status: "failed", errorCode: code, completedAt: new Date() }).where(eq(integrationSyncRuns.id, runId)); await getDb().update(integrationConnections).set({ lastErrorCode: code, updatedAt: new Date() }).where(and(eq(integrationConnections.id, connection.id), eq(integrationConnections.organizationId, context.organizationId))); throw error; } finally { await releaseIntegrationSyncLease(lease); }
  });
}
